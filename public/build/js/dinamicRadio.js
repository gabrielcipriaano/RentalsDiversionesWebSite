function selectTypeContactMethod(){document.querySelectorAll('input[name="contact[type-contact]"]').forEach(e=>e.addEventListener("click",showOpcions))}function showOpcions(e){const a=document.querySelector("#contact");"teléfono"==e.target.value?a.innerHTML='\n    <p>Elija la fecha y la hora para la llamada:</p>\n    <label for="phone">Teléfono:</label>\n    <input type="tel" name="contact[phone]" id="phone" placeholder="Ingresa tu numero de teléfono" required>\n\n    <label for="fecha">Fecha:</label>\n    <input type="date" id="date" name="contact[date] required">\n\n    <label for="time">Indica tu hora preferida para la llamada:</label>\n    <input type="time" name="contact[time]" id="time" required>':a.innerHTML=' \n    <label for="email">Email</label>\n    <input type="email" name="contact[email]" id="email" placeholder="Ingresa tu email" required>\n    '}document.addEventListener("DOMContentLoaded",(function(){selectTypeContactMethod()}));